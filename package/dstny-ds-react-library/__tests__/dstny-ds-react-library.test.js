'use strict';

const dstnyDsReactLibrary = require('..');
const assert = require('assert').strict;

assert.strictEqual(dstnyDsReactLibrary(), 'Hello from dstnyDsReactLibrary');
console.info('dstnyDsReactLibrary tests passed');
